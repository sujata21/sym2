{% extends "AWDynamicTextBundle::layout.html.twig" %}

{% block title %}Dynamic Text{% endblock %}

{% block content -%}
    <h1>FileUpload creation</h1>

    <form action="{{ path('fileupload_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('fileupload') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
